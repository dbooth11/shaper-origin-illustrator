/*
 * shaper.jsx — CEP host entry point. Loaded via manifest ScriptPath.
 * Pulls in the engine so the panel can call applyCut() / tagDepth() / exportForOrigin().
 */
#include "shaper-core.jsxinc"

var SHAPER_VERSION = '1.0.0';
